# Dead, stale duplicate of the web app. The Android app is a Trusted Web
# Activity pointed at the live remote URL (https://mohitto.github.io/MentalMap/,
# see AndroidManifest.xml's DEFAULT_URL meta-data), so nothing in here is ever
# executed. Scanning it only produces duplicate findings against code that cannot run.
android-app/app/src/main/assets/
